{
  "name": "firestore-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Firestore/Storage security rules tests (#285) — run against the local emulator via `firebase emulators:exec`.",
  "type": "commonjs",
  "scripts": {
    "test": "node rules.test.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0"
  }
}
