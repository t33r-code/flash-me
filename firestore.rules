rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isAuth() {
      return request.auth != null;
    }

    function isUser(uid) {
      return isAuth() && request.auth.uid == uid;
    }

    // Each user can only read/write their own profile document.
    match /users/{userId} {
      allow read, write: if isUser(userId);

      // Study sessions are private to the owning user.
      match /studySessions/{sessionId} {
        allow read, write: if isUser(userId);
      }

      // Per-card Skip / Review marks are private to the owning user.
      match /cardMarks/{cardId} {
        allow read, write: if isUser(userId);
      }
    }

    // setCards join documents are private to the owning user.
    // userId is stored on the document so we can verify ownership
    // without reading the parent set or card document.
    match /setCards/{linkId} {
      allow read, update, delete: if isAuth() &&
          resource.data.userId == request.auth.uid;
      allow create: if isAuth() &&
          request.resource.data.userId == request.auth.uid;
    }

    // Cards are private to the user who created them.
    // read/update/delete check the stored createdBy field;
    // create checks the incoming request to prevent spoofing.
    match /cards/{cardId} {
      allow read, update, delete: if isAuth() &&
          resource.data.createdBy == request.auth.uid;
      allow create: if isAuth() &&
          request.resource.data.createdBy == request.auth.uid;
    }

    // Card sets are private to the owning user.
    match /sets/{setId} {
      allow read, update, delete: if isAuth() &&
          resource.data.userId == request.auth.uid;
      allow create: if isAuth() &&
          request.resource.data.userId == request.auth.uid;
    }

    // Templates are private to the user who created them.
    match /templates/{templateId} {
      allow read, update, delete: if isAuth() &&
          resource.data.createdBy == request.auth.uid;
      allow create: if isAuth() &&
          request.resource.data.createdBy == request.auth.uid;
    }
  }
}
