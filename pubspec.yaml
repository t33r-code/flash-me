name: flash_me
description: "A multi-platform flashcard application for language learning."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none'

version: 0.3.2+4

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # Firebase
  firebase_core: ^4.7.0
  firebase_auth: ^6.4.0
  cloud_firestore: ^6.3.0
  firebase_storage: ^13.3.0

  # Google Sign-In
  google_sign_in: ^7.2.0
  google_sign_in_web: ^1.1.3

  # State Management
  riverpod: ^3.2.1
  flutter_riverpod: ^3.3.1

  # UI & Icons
  cupertino_icons: ^1.0.8

  # Markdown rendering (used for card set descriptions)
  flutter_markdown: ^0.7.6

  # File handling
  file_picker: ^11.0.2
  path_provider: ^2.1.0

  # Import / export
  archive: ^3.6.0       # ZIP creation and extraction
  share_plus: ^10.0.0   # Mobile share sheet for exported files

  # Internationalization & Formatting
  intl: ^0.20.2

  # Local Storage
  shared_preferences: ^2.2.0

  # Logging
  logger: ^2.1.0

  # HTTP (for API calls if needed)
  http: ^1.1.0

  # URL launching (used for in-app help links)
  url_launcher: ^6.3.0

  # Connectivity
  connectivity_plus: ^7.1.1
  web: ^1.1.1
  firebase_crashlytics: ^5.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.0

  # Testing
  test: ^1.25.0
  mockito: ^5.4.0
  build_runner: ^2.4.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  generate: true  # enables flutter gen-l10n to run automatically on flutter build / test

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

flutter_launcher_icons:
  android: true
  ios: true
  remove_alpha_ios: true                         # iOS forbids alpha; strip it from the store icon
  image_path: "assets/icon/icon.png"             # flat full-bleed icon (legacy Android, iOS, web, desktop)
  adaptive_icon_background: "#CCD5FF"             # lavender backing layer for Android adaptive icons
  adaptive_icon_foreground: "assets/icon/foreground.png"  # artwork inset into the central safe zone
  web:
    generate: true
    image_path: "assets/icon/icon.png"
  windows:
    generate: true
    image_path: "assets/icon/icon.png"
  macos:
    generate: true
    image_path: "assets/icon/icon.png"
