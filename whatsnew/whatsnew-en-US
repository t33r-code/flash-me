Alpha 0.8

Cards now behave the same way everywhere: tap to open one, long-press (or right-click) for actions like Clone. Long-press no longer accidentally starts a multi-select. Also fixes an issue where another account's email could be exposed through a set's author name in the Market.
